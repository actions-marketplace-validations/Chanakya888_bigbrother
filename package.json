{
  "name": "bigbrother",
  "version": "1.0.0",
  "description": "Context-aware PR reviews using Claude + PR-Agent",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --license licenses.txt",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@anthropic-ai/sdk": "^0.30.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
